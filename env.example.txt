# Google Cloud Platform Configuration
# Copy this file to .env and fill in your values
# The setup scripts will do this automatically

# Your GCP Project ID
GOOGLE_CLOUD_PROJECT=your-project-id

# GCP Region/Location (e.g., us-central1, us-east1, europe-west1)
GOOGLE_CLOUD_LOCATION=us-central1

# Optional: Path to service account key file (if using service account)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Optional: Agent configuration
AGENT_NAME=hello-world-agent
AGENT_DISPLAY_NAME=Hello World Agent

# Google Gemini API (simpler alternative to Vertex AI)
# Get free API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-pro

# Ollama Configuration (for truly local AI - install from https://ollama.ai)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2

